{% comment %} 
=============================================================================
ГЛОБАЛЬНЫЙ МОДУЛЬ ОПТИМИЗАЦИИ МЕДИА (media-logic.liquid)
Назначение: Подключение оригинального закешированного скрипта ленивых видео-плееров.
=============================================================================
{% endcomment %}

<!-- Подключаем только ОДИН оригинальный изолированный скрипт ленивого видео -->
<script src="{{ '/assets/js/video-lazy-load.js' | relative_url }}"></script>

<script>
  // Запускаем ленивые интерактивные плееры для видеофайлов (.webm) строго после полной сборки страницы браузером
  document.addEventListener("DOMContentLoaded", function() {
    if (typeof runVideoLazyLoad === 'function') {
        runVideoLazyLoad();
    }
  });
</script>
